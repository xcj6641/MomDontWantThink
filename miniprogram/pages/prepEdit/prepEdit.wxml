<view class="container">
  <view class="section" wx:if="{{prep}}">
    <view class="label">备餐</view>
    <text class="value">{{prep.recipeName}}</text>
  </view>
  <view class="section" wx:if="{{prep}}">
    <view class="label">分配日期</view>
    <text class="value">{{prep.assignedDateText}}</text>
  </view>
  <view class="tip" wx:if="{{!prep}}">未找到该备餐数据（仅 Mock 联调）</view>
</view>
