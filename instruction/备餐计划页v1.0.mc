AI 开发指令：备餐计划页（weekPlan）完整规格 v1.0
1. 页面目标

该页面用于：

展示本周辅食计划

展示并调整本周备餐设置

展示并调整日期分配

编辑备餐内容

重新生成某组备餐（换一组）

删除某组备餐

点击某一天，进入单日编辑页，调整某一天某一餐

用户确认后开始本周计划，并回到辅食首页常规界面

2. 页面整体结构（从上到下）

页面结构固定为：

Header
本周备餐设置区
日期分配区
备餐卡片列表
底部确认按钮
3. Header
3.1 标题
✔️ 已帮你准备好本周辅食计划
3.2 副标题
按宝宝 {babyAgeMonths} 月龄生成，可随时调整

示例：

✔️ 已帮你准备好本周辅食计划
按宝宝 11 月龄生成，可随时调整
4. 本周备餐设置区
4.1 UI
📦 本周备餐设置

本周备 3 套菜单   [调整]
4.2 说明

这里的数量表示：本周共生成多少组“备餐”

默认值：3

支持范围：1 ~ 7

4.3 点击 [调整] 后弹出底部选择框
标题
本周准备几套备餐？
选项
1 套
2 套
3 套
4 套
5 套
6 套
7 套
按钮
取消   确认
5. 调整备餐数量规则
5.1 当新数量 > 当前数量

先弹出二次确认框：

新增备餐后，是否自动重新分配日期？

☑ 自动重新分配日期
系统会按新的备餐数重新安排一周日期

取消   确认
若勾选“自动重新分配日期”

系统执行：

新增备餐

自动生成新增备餐的所有餐次内容

重新计算 7 天的日期分配

若未勾选

系统执行：

新增备餐

自动生成新增备餐的所有餐次内容

保持原日期分配不变

新增备餐默认为 暂未使用

5.2 当新数量 < 当前数量

系统删除最后的备餐。

例如：

从 5 套改成 3 套

删除 备餐4、备餐5

如果被删除的备餐绑定了日期：

这些日期需要自动重新分配

重新分配规则：优先分配给当前天数最少的备餐

6. 日期分配区
6.1 UI 标题
日期分配（2-3-2）
已为你安排好，可选择备餐调整日期

说明：

(2-3-2) 表示当前每组备餐覆盖的天数分布

如果分配不连续，也按每组天数显示，例如 (4-3-0)

7. 备餐选择按钮
7.1 UI
● 备餐1   ● 备餐2   ● 备餐3
7.2 规则

横向排列

当数量较多时，支持横向滚动

当前选中的备餐高亮显示

每个按钮颜色与下面对应卡片颜色一致

颜色建议：

备餐1 = 绿色

备餐2 = 蓝色

备餐3 = 橙色

备餐4 = 紫色

备餐5 = 青色

备餐6 = 粉色

备餐7 = 灰蓝色

8. 日期分配行
8.1 UI
一 二 三 四 五 六 日
🟢 🟢 🔵 🔵 🔵 🟠 🟠
8.2 状态规则
未分配日期

白底

灰边框

已分配但不属于当前选中备餐

显示该备餐的浅色背景

属于当前选中备餐

显示当前备餐的深色背景

白字

9. 日期分配交互

用户操作：

点击某个备餐（例如 备餐2）

点击某一天（例如 周四）

系统行为：

将周四分配给备餐2

如果周四原本属于其他备餐，则自动转移

提示：

周四已调整为备餐2
10. 日期分配规则

系统只要求：

每一天都必须分配给某个备餐

系统不要求：

每个备餐都必须至少分配一天
11. 允许“暂未使用”的备餐

例如：

本周共 7 套备餐

但只给其中 5 套分配了日期

另外 2 套没有日期

这是允许的。

未使用的备餐仍然显示在页面中，状态写为：

暂未使用
12. 备餐卡片列表

每个备餐显示一张卡片。

12.1 卡片标题区域

格式：

备餐1   周一–周二      ⋯

或：

备餐2   周三、周五      ⋯

或：

备餐4   暂未使用      ⋯
12.2 日期显示规则

卡片标题里的日期需要尽量压缩显示：

连续日期合并

周一、周二、周三 → 周一–周三

不连续日期

周二、周四、周六 → 周二、周四、周六

全周

周一到周日 → 周一–周日

如果文字仍然过长：

最多显示一行

超出部分省略号 ...

13. 卡片右上角三点菜单

点击 ⋯，弹出操作菜单：

编辑备餐
换一组
删除备餐
14. 三点菜单功能定义
14.1 编辑备餐

进入 备餐编辑页（templateEdit）

作用：

修改该备餐的餐次内容

修改某个餐次是否为 BLW

注意：

编辑备餐会影响所有使用该备餐的日期

14.2 换一组

作用：

重新生成当前备餐的整组内容

保留该备餐的日期分配

保留餐次结构

保留 BLW 设置

重新生成每个餐次的菜品

若该备餐已有单日调整（override）：
弹窗确认：

换一组后，这组备餐的单日调整会被清除

取消   继续

点击继续后：

清除该备餐所有相关日期上的 override

生成新的备餐内容

成功提示：

已为你换了一组新的备餐
14.3 删除备餐

点击后弹窗：

删除备餐2？

取消   删除

无需额外说明文案。

删除后：

移除该备餐

若该备餐绑定了日期，则这些日期自动重新分配

后续备餐重新编号（例如备餐3 → 备餐2）

成功提示：

已删除备餐2

规则：

至少保留 1 套备餐

当只有 1 套备餐时，不显示“删除备餐”

15. 备餐卡片内容
15.1 根据月龄动态显示餐次

餐次由宝宝月龄决定。

例如：

6 月龄

只显示：

午餐
9 月龄

显示：

早餐
午餐
晚餐
11 月龄

显示：

早餐
加餐1
午餐
加餐2
晚餐
15.2 卡片内容示例
早餐：紫米粥
加餐1：香蕉
午餐：鸡肉胡萝卜软饭
加餐2：苹果
晚餐[BLW]：西兰花、肉条
16. 菜品名称过长时的显示规则

如果某一餐的菜品名称过长：

最多显示两行

超出部分省略号 …

例如：

晚餐[BLW]：西兰花、牛肉条、胡萝卜条、
南瓜条、土豆条…

点击“编辑备餐”后可查看完整内容。

17. BLW 标识规则

BLW 标签固定位置：

餐次名称后，冒号前

示例：

晚餐[BLW]：西兰花、肉条

不允许：

[BLW]晚餐：...
晚餐：...[BLW]
18. 单日单餐调整（override）
18.1 入口

用户点击备餐卡片标题中的某个日期（例如 周二）：

进入：

单日编辑页（dayEdit）
18.2 单日编辑页作用

只修改某一天的某个餐次菜品。

可修改的餐次由月龄决定，例如：

早餐

加餐1

午餐

加餐2

晚餐

修改后只影响当天。

19. 单日调整在备餐卡片中的显示

如果某一天某餐被修改，则对应备餐卡片中要显示 override 摘要。

示例：

模板默认：

加餐1：香蕉

周二加餐1 被改成 梨

卡片显示：

加餐1：香蕉｜周二：梨

如果多个日期都改了：

加餐1：香蕉｜周二：梨；周四：牛油果

说明：

香蕉 = 默认菜品

周二：梨 = 单日替换结果

20. 自动分配算法
20.1 初始化生成时

当系统第一次按 N 套备餐分配 7 天：

base = 7 // N
extra = 7 % N

前 extra 个备餐：

分配 base + 1 天

剩余备餐：

分配 base 天

示例：

N = 3

3, 2, 2

N = 4

2, 2, 2, 1

N = 7

1, 1, 1, 1, 1, 1, 1

20.2 删除备餐后重新分配

若某个被删除的备餐有日期绑定：

将这些日期分配给当前天数最少的备餐

直到所有日期重新分配完成

21. 页面底部确认按钮

按钮位于页面最底部，不悬浮。

文案：

确认并开始本周计划

点击后：

保存本周备餐内容

保存日期分配

保存 override

写入 weekSettings.confirmed = true

返回辅食首页（常规界面）

成功提示：

好的，本周计划开始啦
22. 页面数据结构建议
22.1 weekPlan
{
  "weekStartDate": "2026-03-09",
  "menuCount": 3,
  "dayAssignments": {
    "mon": 1,
    "tue": 1,
    "wed": 2,
    "thu": 2,
    "fri": 2,
    "sat": 3,
    "sun": 3
  }
}
22.2 mealTemplate（建议命名可内部保留 template，但 UI 用“备餐”）
{
  "templateId": 1,
  "name": "备餐1",
  "days": ["mon", "tue"],
  "meals": {
    "breakfast": {
      "recipeId": "r1",
      "name": "紫米粥",
      "blw": false
    },
    "snack1": {
      "recipeId": "r2",
      "name": "香蕉",
      "blw": false
    },
    "lunch": {
      "recipeId": "r3",
      "name": "鸡肉胡萝卜软饭",
      "blw": false
    },
    "snack2": {
      "recipeId": "r4",
      "name": "苹果",
      "blw": false
    },
    "dinner": {
      "recipeId": "r5",
      "name": "西兰花、肉条",
      "blw": true
    }
  }
}
22.3 override
{
  "day": "tue",
  "mealType": "snack1",
  "recipeId": "r100",
  "recipeName": "梨"
}
23. 必须满足的规则总结

页面顶部展示本周备餐设置

备餐数量支持 1–7

增加备餐时提供“自动重新分配日期”勾选框

日期分配允许用户手动调整

每一天必须有备餐

备餐允许暂未使用

卡片标题显示压缩后的日期范围

卡片右上角使用三点菜单

三点菜单包含：编辑备餐 / 换一组 / 删除备餐

删除备餐时至少保留 1 套

备餐卡片按月龄动态显示餐次

BLW 标签固定写在餐次后、冒号前

单日改单餐后，卡片显示 默认菜品｜周X：新菜品

页面底部按钮确认后开始本周计划并返回首页