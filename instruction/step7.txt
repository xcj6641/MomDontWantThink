请完成前端与云函数联调，使以下闭环可运行：
1) 首次进入home：调用initUser；若无本周数据则生成本周（或提示生成下周再自动切本周，你自行选最简策略，但要能立即看到“今日安排”）。
2) home展示今日菜单；点击“✔ 标记已做”写meal_logs；在logs页可看到记录，并可标记异常。
3) 进入本周计划week：能看到模板列表+使用日期；进入templateEdit修改模板后返回生效。
4) weekSettings：调整N套+日期分配；确认时校验空白天，支持自动分配；保存后week页刷新。
5) 生成下周：调用generateNextWeek；nextWeek页可查看；shopping页可看到购物清单已备X/共Y并勾选。
要求：
- 封装一个 utils/cloud.js，统一调用云函数（带loading、错误toast、返回数据处理）。
- 所有云函数调用必须携带必要参数（weekStartDate等）。
- 保证最少报错、流程可跑通。
输出：需要修改/新增的内容更新到文件中。